export enum todoFieldNames {
  title = 'title',
  description = 'description',
  dueDate = 'dueDate',
  coverImage = 'coverImage',
  status = 'status'
}

export enum taskStatuses {
  todo = 'todo',
  done = 'done',
  removed = 'removed'
}