
import { StyleSheet } from "react-native";


export const loginStyles = () =>
  StyleSheet.create({
    scrollContainer: {
      opacity: 1,
    },
    screenContainer: {
      paddingBottom: 0,
    },
  });
